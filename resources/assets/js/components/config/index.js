export const storyDraggableOptions = {
    group: "stories",
    animation: 150,
    forceFallback: false,
    fallbackOnBody: false,
    fallbackTolerance: 0,
    scroll: true,
    scrollSpeed: 10
};

export const groupsDraggableOptions = {
    group: "groups",
    handle: ".group-handle",
    animation: 150,
    forceFallback: false,
    fallbackOnBody: false,
    fallbackTolerance: 0,
    scroll: true,
    scrollSpeed: 10
}